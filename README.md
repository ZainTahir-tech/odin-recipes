I have to make html files and navigate through different html files using the html I have learned so far like:

    links using anchor tag
    images using img tag
    headings h tags
    paragrahs p tag

I will also use the boilerplate code like meta information etc.